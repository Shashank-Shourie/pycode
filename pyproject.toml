[project]
name = "pycode"
version = "0.1.0"
description = "A simple AI coding agent built in Python"
requires-python = ">=3.11"
dependencies = [
    "groq",
    "typer",
    "rich",
    "python-dotenv",
]

[project.scripts]
pycode = "pycode.main:main"